<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Blogs</title>
    <%- include('partials/head-links') %>
</head>
<body>
    <div class="d-flex align-items-center">
        <a href="/admin" class="btn btn-primary ms-4">Waitlist</a>
        <h3 class="text-center my-4 mx-auto">All Blogs</h3>
        <a href="/admin/new-blog" class="btn btn-primary me-4">New Blog</a>        
    </div>
    <% if (blogs) { %>
        <div class="container-fluid mb-5 row">
            <% blogs.forEach(blog =>{ %>
                <div class="mx-2 col">
                    <img src="<%= blog.image %>" alt="" width="200" height="200">
                    <div class="">Title: <b><%= blog.title %> </b></div>
                    <div class="">Date: <b><%= blog.date %> </b></div>
                    <div class="">Description: <b><%= blog.summary %> </b></div>
                    <div class="">Category: <b><%= blog.category %> </b></div>
                    <div class="mb-3">
                        <a class="btn btn-primary btn-sm" href="/edit/<%= blog.id %>">Edit</a>
                    </div>
                </div>
            <% }) %> 
        </div>
    <% } %>
</body>
</html>